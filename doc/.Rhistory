packages.used = c('tm', 'wordcloud', 'RColorBrewer','dplyr', 'tidytext')
packages.used
install.packages()
installed.packages()
installed.packages()[,1] -> po
po
setdiff(packages.used,po)
setdiff(packages.used, intercept(po,packages.used))
setdiff(packages.used, intersect(po,packages.used))
packages.uesd
package.uesd
packages.uesd
packages.used
packages.needed=setdiff(packages.used,
intersect(installed.packages()[,1],
packages.used))
library(packages.used)
library(as.vector(packages.used))
folder.path="../data/inaugurals/"
speeches=list.files(path = folder.path, pattern = "*.txt")
prex.out=substr(speeches, 6, nchar(speeches)-4)
ff.all<-Corpus(DirSource(folder.path))
packages.used=c("tm", "wordcloud", "RColorBrewer",
"dplyr", "tidytext")
# check packages that need to be installed.
packages.needed=setdiff(packages.used,
intersect(installed.packages()[,1],
packages.used))
# install additional packages
if(length(packages.needed)>0){
install.packages(packages.needed, dependencies = TRUE,
repos='http://cran.us.r-project.org')
}
library(tm)
library(wordcloud)
library(RColorBrewer)
library(dplyr)
library(tidytext)
folder.path="../data/inaugurals/"
speeches=list.files(path = folder.path, pattern = "*.txt")
prex.out=substr(speeches, 6, nchar(speeches)-4)
ff.all<-Corpus(DirSource(folder.path))
help(list.files)
help("Corpus")
help("DirSource")
help(system.file)
help('help')
system.file('help')
folder.path = '../data/InauguralSpeeches/'
filenames = list.files(path=folder.path, pattern='*.txt')
speeches = substr(filesnames, nchar(filenames)-4)
folder.path = '../data/InauguralSpeeches/'
filenames = list.files(path=folder.path, pattern='*.txt')
speeches = substr(filenames, nchar(filenames)-4)
help("substr")
folder.path = '../data/InauguralSpeeches/'
filenames = list.files(path=folder.path, pattern='*.txt')
speeches = substr(filenames, 1, nchar(filenames)-4)
ff.all<-Corpus(DirSource(folder.path))
ff.all<-tm_map(ff.all, stripWhitespace)
ff.all<-tm_map(ff.all, content_transformer(tolower))
ff.all<-tm_map(ff.all, removeWords, stopwords("english"))
ff.all<-tm_map(ff.all, removeWords, character(0))
ff.all<-tm_map(ff.all, removePunctuation)
tdm.all<-TermDocumentMatrix(ff.all)
tdm.tidy=tidy(tdm.all)
tdm.overall=summarise(group_by(tdm.tidy, term), sum(count))
ff.all
help("TermDocumentMatrix")
help(tidy)
tdm.tidy
\
tdm.overall
term()
tdm.tidy
help("group_by")
group_by(tdm.tidy, term)
summarise(tdm.tidy, sum(count()))
summarise(tdm.tidy, sum(count)
)
folder.path = '../data/InauguralSpeeches/'
filenames = list.files(path=folder.path, pattern='*.txt')
speeches = substr(filenames, 1, nchar(filenames)-4)
ff.all <- Corpus(DirSource(folder.path))
ff.all <- tm_map(ff.all, stripWhitespace)
ff.all <- tm_map(ff.all, content_transformer(tolower))
ff.all <- tm_map(ff.all, removeWords, stopwords("english"))
ff.all <- tm_map(ff.all, removeWords, character(0))
ff.all <- tm_map(ff.all, removePunctuation)
tdm.all <- TermDocumentMatrix(ff.all)
tdm.tidy <- tidy(tdm.all)
tdm.overall <- summarise(group_by(tdm.tidy, term), sum(count))
)
tdm.overall
help(wordcloud)
help("DocumentTermMatrix")
#td-idf
dtm <- DocumentTermMatrix(ff.all,
control = list(weighting = function(x)
weightTfIdf(x,
normalize =FALSE),
stopwords = TRUE))
ff.dtm=tidy(dtm)
ff.dtm
ff.dtm[1:50,]
view(dtm)
view(mtd)
